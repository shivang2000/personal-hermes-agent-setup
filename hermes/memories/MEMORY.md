For Discord bot-to-bot messages in Shivang's Hermes setup, use real mention tokens, not plain names: Office Hermes / `hermes-work-agent` is `<@1516899802913308733>`; Personal Hermes / this bot is `<@1516782563757133964>`.
§
Production macOS computer-use implementations cloned at ~/.hermes/research/computer-use-macos/ for reference. Open-codex-computer-use is the highest-value repo (reverse-engineered OpenAI Codex Computer Use, has OpenComputerUseKit Swift source). Other useful ones: desktop-pilot-mcp (cleanest Swift layer-router architecture), native-devtools-mcp (AX snapshot invalidation patterns), ghost-os (self-learning recipes + AX-first), agent-computer-use (Rust crates per platform). Skill at apple/macos-computer-use-implementations summarizes the proven patterns (AX-first routing cascade, CGEvent.postToPid background safety, xdotool key parser, recipe-based workflows, permission state machine).
§
Shivang's trading bot setup: ~/dev/advanced-trading-bot/trading-bot-v2/ — FundingPips $5K 1 Step account. Config: config/fundingpips-5k.yaml (bot DD 2%/4.5%, FP 3%/6%). MT5 server: "FundingPips Corp (2)" (live) or "FundingPips-SIM1" (demo). Discord alerts: #trading-bot channel (ID 1525476646176690297) via DISCORD_BOT_TOKEN + DISCORD_ALERT_CHANNEL_ID env vars (bot-token mode, posts as hermesagent#5141). Docker: docker compose -f docker-compose.ec2.yml up -d on EC2 (start trading-bot AFTER MT5 logged in via noVNC). CONFIG_OVERLAY=REDACTED_SET_LOCALLY

EC2: i-095ac6e00402283e5 (ap-south-1, public IP 3.7.59.175), Amazon Linux 2023, 2 vCPU 2GB RAM 30GB disk. Username ec2-user (root login disabled). PEM at /Users/shivang/dev/advanced-trading-bot/trading-bot-v2/south-mumbai-key-pair.pem. noVNC at http://3.7.59.175:8080 (pwd botpass) for MT5 GUI login. RPyC port 8001 for bot↔MT5. SG: default, launch-wizard-1, launch-wizard-2 — user must open 8080 + 8001 from their IP (49.36.99.137) to access noVNC. Demo creds: account 20349333 / pwd lQ3g?7l.> / server FundingPips-SIM1.

EC2 deploy pitfalls (learned 2026-07-12): (1) `mt5_data` bind-mount dir must be chown 911:911 BEFORE first start — otherwise Wine says "is not owned by you" and curl downloads fail (`sudo chown -R 911:911 mt5_data` from EC2-user shell works; ec2-user is in wheel+sudo NOPASSWD). (2) Box has `docker-compose` v5.0.1 standalone (not `docker compose` plugin) and buildx 0.12 — incompatible. Workaround: `docker build -t trading-bot-v2-trading-bot:latest .` directly, THEN `docker-compose -f docker-compose.ec2.yml up -d`. (3) IMDSv1 is disabled (returns 401), need IMDSv2 token to query instance metadata. (4) The bot crash-loops on "MT5 result expired" when MT5 has no logged-in account — must log in via noVNC FIRST, then start the bot. (5) Main.py fatal-exits when both MT5 and Telegram are down. Telegram isn't configured for this deploy (SignalGenerator strategies are self-contained, no signal channels), so the Telegram-skip is expected and not a problem.
§
Shivang's Hermes default model is now GLM 5.2 via ollama-cloud (switched 2026-07-11 from minimax-m3). He has an Ollama Pro plan and wants GLM 5.2 for everything — main + delegation. Fallback stays openai-codex/gpt-5.6-sol for outages. Config changed via `hermes config set` (not patch tool — security guard blocks direct config.yaml edits).
§
Shivang has high risk tolerance for trading — overrides safety recommendations (e.g. insisted on going straight to funded account, skipping demo validation). Still apply safety guardrails in code/config, but don't block — document the override and make the bot tighter than the prop firm's limits instead.
§
Shivang prefers local execution over cloud when possible — tried multiple approaches (Docker+Rosetta, Docker+QEMU, native MT5 Wine) before accepting EC2. Always attempt local-first paths before suggesting cloud deployment.
§
EC2: i-095ac6e00402283e5 (t3.medium equivalent, ap-south-1, IP 3.7.59.175), noVNC port 8080 pwd botpass. MT5 cannot run on Apple Silicon (Rosetta GDT error + QEMU illegal instruction). Use EC2. Old instance i-0c7e31141a9bd1de6 is gone.