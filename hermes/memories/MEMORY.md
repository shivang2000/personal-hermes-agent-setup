Recurring Hermes cron jobs for Shivang should be pinned to openai-codex/gpt-5.5 unless he explicitly requests a different model.
§
For Discord bot-to-bot messages in Shivang's Hermes setup, use real mention tokens, not plain names: Office Hermes / `hermes-work-agent` is `<@1516899802913308733>`; Personal Hermes / this bot is `<@1516782563757133964>`.
§
Production macOS computer-use implementations cloned at ~/.hermes/research/computer-use-macos/ for reference. Open-codex-computer-use is the highest-value repo (reverse-engineered OpenAI Codex Computer Use, has OpenComputerUseKit Swift source). Other useful ones: desktop-pilot-mcp (cleanest Swift layer-router architecture), native-devtools-mcp (AX snapshot invalidation patterns), ghost-os (self-learning recipes + AX-first), agent-computer-use (Rust crates per platform). Skill at apple/macos-computer-use-implementations summarizes the proven patterns (AX-first routing cascade, CGEvent.postToPid background safety, xdotool key parser, recipe-based workflows, permission state machine).
§
The cron `office-work-summary-for-tweets` (job_id `81a56cfa2830`, weekdays 7:30 PM IST) is owned by Shivang's office-laptop/work-agent Hermes profile, NOT by the personal/default profile. When its digest lands in a shared channel like `#tweets-automation`, the personal-Hermes bot receiving it should treat it as a passive data drop — it cannot resume/edit that job, and should wait for full digest (paginated messages like `(1/6)`) before drafting anything from it. Downstream issues the digest reveals (e.g. paused autopost crons) usually belong to the receiving profile and are worth flagging.
§
Shivang's default-profile X autopost cron fleet (`cdd18a71e079` daily post, `24fd916089e3` source-backed scan, `6be19c7edbdb` news digest, `e9f85b48274e` reply autopilot) all share a single X API tier quota. An `every 5m` reply scanner is a quota bomb — it can burn the monthly cap in well under 24h and silently 429 every other X-facing job. When the fleet hits `HTTP 429: The usage limit has been reached`, the scheduler auto-pauses all of them; recovery requires fixing cadence AND topping up credits AND `hermes cron resume <id>` for each, one at a time. Default safe cadences: reply scanner `every 30m`, opportunity scan `every 45m-2h`, news digest `every 4h`, daily queue `1/day`.
§
Shivang has a FundingPips $5K 1 Step account. Bot code at ~/dev/advanced-trading-bot/trading-bot-v2/. FP 1 Step rules: 10% profit target, 3% daily DD, 6% overall DD, 3 min trading days, 1:30 leverage (metals 1:20), news allowed on Step 1 / restricted on Master (5-min window), EAs allowed as trade/risk manager. MT5 server: "FundingPips Corp (2)". Discord alert channel: #trading-bot (ID 1525476646176690297) on Shivang's Hermes Discord server. Bot uses DISCORD_BOT_TOKEN + DISCORD_ALERT_CHANNEL_ID env vars to post as hermesagent#5141. Config: fundingpips-5k.yaml (bot trips at 2%/4.5% DD, FP at 3%/6%). Post-mortem at docs/post-mortem-5k.md — previous $5k bust was manual-trade-during-news on Step 2. 240/240 tests pass.
§
Shivang's trading bot setup: ~/dev/advanced-trading-bot/trading-bot-v2/ — FundingPips $5K 1 Step account. Config: config/fundingpips-5k.yaml (bot DD 2%/4.5%, FP 3%/6%). MT5 server: "FundingPips Corp (2)". Discord alerts: #trading-bot channel (ID 1525476646176690297) via DISCORD_BOT_TOKEN + DISCORD_ALERT_CHANNEL_ID env vars (bot-token mode, posts as hermesagent#5141). Docker: ./scripts/run.sh up -d (auto-detects macbook vs ec2 overlay). CONFIG_OVERLAY=REDACTED_SET_LOCALLY
§
Shivang's Hermes default model is now GLM 5.2 via ollama-cloud (switched 2026-07-11 from minimax-m3). He has an Ollama Pro plan and wants GLM 5.2 for everything — main + delegation. Fallback stays openai-codex/gpt-5.6-sol for outages. Config changed via `hermes config set` (not patch tool — security guard blocks direct config.yaml edits).